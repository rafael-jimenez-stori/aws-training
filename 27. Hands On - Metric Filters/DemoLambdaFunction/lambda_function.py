import json
import time

def lambda_handler(event, context):
   # try:
   #    # Call dependency
   #    dependencyClient.uploadFile(...)
   # except ClientError:
   #    #Catch the exception and emit a metric
   #    #cloudWatchClient.putMetric("UploadError", 1)
   print("UploadError 1")
